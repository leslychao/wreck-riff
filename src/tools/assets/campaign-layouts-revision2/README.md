# Archived revision 2

These original layout and scenery sources and generated outputs were archived before
the revision-three reauthoring. They are historical assets, excluded from runtime
resources. The original static-art renderer is retained as `ArenaArt.java.txt`.

Revision three replaces grid-filled blocks, shared long hall plans, and asphalt
overlays emitted from navigation edges. No deleted original authoring work is lost.
See `provenance.json` for file checksums captured after archiving.
