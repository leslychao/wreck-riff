# Приёмка расширенного Windows-релиза

Проверяется [RELEASE_PLAN](RELEASE_PLAN.md), состояние на 13 сентября 2026 года.
Итоговая поставка пока не принята. Рабочая копия меняется, поэтому перечисленные
ниже промежуточные проверки не заменяют чистый прогон окончательного снимка.

Предыдущая приёмка оружейной версии 0.4, её ZIP/SHA и демонстрация сохранены в
[истории](history/ACCEPTANCE-0.4-before-campaign.md). Они не относятся к будущему
пакету кампании и трёх шасси.

## Подтверждённые промежуточные проверки

| Проверка | Результат и ограничение |
|---|---|
| Unit до последних диагностических/аудио изменений | 508 PASS; не итоговый снимок |
| Повторная проверка управления и HP, 13 сентября | 554 unit + 2161 native PASS, verifyAssets TECHNICAL_PASS (231 файл); [точный снимок и замеры](ARCADE_VERIFICATION_2026-09-13.md). Это не проверка окончательного ZIP |
| Подбросы трёх шасси и окружение | 1882 native PASS; 10 платформ, нормализованные скорости включая turbo, углы 0/±25/±50°, 30/60/120 Гц и дополнительные сценарии |
| Контактные механизмы | 8 native PASS после исправления учёта боковых и предварительных контактных импульсов; дополнительная скорость ограничена 8 м/с |
| AI и маршруты боссов | 33 unit + 46 native PASS; финальное восстановление назначения Директора после ремонта проверено дополнительным native-сценарием |
| Десять обычных боёв | Максимальный неплановый простой 278 < 600 ticks; seed 9 / id 2 — 45 ticks; неподтверждённые recovery 0,333 на 10 bot-minutes |
| Обычный запуск / профилирование | Unit-проверки LaunchReport и ограниченных асинхронных GPU-запросов PASS; реальный финальный EXE ещё не проверен |
| Release tooling | Проверены parser, положительные/отрицательные fixtures, идентичность ZIP/артефактов и длинный путь BAT; окончательный пакет ещё не запущен |

Направленные журналы находятся в `build/arena-full-roster-launch.log`,
`build/arena-native-contact-cap.log`, `build/boss-native-final-verification.log`,
`build/boss-relocation-supply-regression.log`, `build/normal-launch-evidence.log`
и `build/gpu-profiling-checks.log`. При финальной проверке evidence экспортируется
с привязкой к точному ZIP и исходникам; изменяемые файлы build не являются такой
привязкой сами по себе.

## Незавершённые проверки и известные сбои

- Короткий реальный soak на 110 секунд завершился по тайм-ауту после повторной
  загрузки (`build/soak-smoke-20260913.log`). Причина остановки пока не установлена.
  Это FAIL, а не подтверждение длительного прогона. Добавлено сохранение стеков
  потоков при тайм-ауте для следующей диагностики.
- Два прежних падения выезда Grinder оказались вычитанием обратного и прямого
  движения в измерении общего смещения. Исправленная проверка отдельно измеряет
  секунду реальной тяги вперёд после торможения, проверяет скорость и опору;
  оба сценария прошли в полном прогоне 2161 native-теста указанного снимка.
- Ошибка jME audio decoder при освобождении пяти потоковых буферов воспроизведена
  и исправлена в узкой границе запроса устройства. Старый graphical PASS с
  исключением фонового потока не считается чистым завершением; требуются новые окна.
- Финальная графическая матрица всех арен, интерфейса и предупреждений боссов,
  полный benchmark 6 × (30 секунд прогрева + 600 секунд боя), 30-минутный soak
  с минимум 10 сменами карт и 20 Retry — PENDING.
- Чистые test / physicsTest / verifyAssets, упаковка и повторные проверки EXE из
  окончательного ZIP — PENDING. ZIP предыдущей версии здесь не используется.
- Комплект соответствующих исходников и уведомлений OpenAL/Microsoft OpenJDK
  подготавливается и должен пройти offline-проверку и проверку состава ZIP.

## Ручная приёмка конкретного кандидата

- Новая установка, мигрированный профиль, сохранение/продолжение кампании,
  интерфейс и видеонастройки: PENDING.
- Физический геймпад, переназначение и отключение устройства: PENDING_MANUAL.
- Отдельная чистая Windows без внешней Java: PENDING_MANUAL. Локальный изолированный
  запуск не заменяет проверку другой установки ОС.
- Читаемость в движении, звук, ощущения от управления и боя: PENDING_OWNER.
- Комплектность документации/лицензий и окончательная приёмка: PENDING.

Технический release gate сохраняет эти границы и не создаёт подтверждения
владельца автоматически. Публикация и развёртывание не выполнялись.

FUNCTIONAL_COMPLETE = NO (remaining runtime and release checks)

FEEL_APPROVED = NO (owner review pending)

MVP_ACCEPTED = NO (owner and remaining manual gates pending)
