# Художественное обновление: интеграция и приёмка

Локальное оформление всех шести арен загружается через `ArenaArt` по записи
`ArenaRegistry`; освещение и свечение переключаются существующим `SceneLighting`.
Профили машин определяют габариты моделей, точки оружия и эффекты повреждений.
Процедурные исходники и прежние рецепты сохраняются в проекте вместе с происхождением
ресурсов. Сборка использует локальные файлы и не загружает ресурсы из сети.

`ArenaSystems` остаётся владельцем доступности и выдачи подборов. Его `PICKUP`
передаёт session ID, стабильный ID площадки, получателя, тип и фактическое количество.
`PickupStyle` связывает восемь экспортированных моделей с дорожными символами и HUD.
`PickupPresentation` анимирует модели по времени симуляции: вращение 12 с,
покачивание ±0,15 м за 2,4 с, схлопывание 24 тика, появление 30 тиков.
`PickupFeedback` показывает до трёх сообщений на 144 тика и подсветку слота на 48 тиков;
подбор не переключает оружие. Восстановление checkpoint и возрождение сами по себе
не создают подтверждение получения.

`AudioDirector.startMatch(UUID, normal, boss)` явно привязывает звуковой конвейер
к матчу. Старые и не привязанные к матчу события не воспроизводятся и не занимают
историю дедупликации. После остановки разрешены только финальные звуки этого матча.
Шесть оружейных банков содержат по три варианта; собственный сигнал непозиционный,
чужой тише и зависит от расстояния. Общий предел 32 источника включает обе дорожки
при переходе к музыке босса. Подборы имеют меньший приоритет, чем предупреждения
и работающие опасности. Подробности — в [pickup-audio.md](pickup-audio.md).

`ArenaPresentation` связывает сцену с тем же `ArenaSystems`: стрелки подброса
показывают реальное сжатие платформы, панели исчезают только после открытия
физического прохода, а барьер видим только при активной коллизии. Декор разрушаемого
объекта сгруппирован отдельно от соседних фасадов. `ArenaMechanismPresentation`
читает готовые `MechanismView`: положение и поворот груза, карусели и служебного
автомобиля не вычисляются вторым таймером. Полноразмерные модели остаются внутри
соответствующих физических габаритов.

По дополнительному замечанию владельца об угловатых областях огня из изображения
убрана метровая клетчатая обводка. Мягкие пятна следуют реальным поддержанным
участкам поверхности. Тонкие предупреждения баллистики сохраняют радиус попадания,
получают место в ограниченном буфере раньше декоративной копоти и рисуются поверх
неё. Снижение интенсивности вспышек не скрывает действующие опасности.

## Проверки

13 сентября повторная интеграция `BossActionPresentation` подключена к composition
root: создание после checkpoint, обновление после повреждений и фаз босса, очистка
до остановки аудио. Предупреждение читает настоящую фазу `TELEGRAPH` из AI; первый
снимок восстановленного действия не запускает звук. Свет плавно нарастает на
существующих фарах, не добавляет геометрию и не открывает уязвимую панель.

Изолированный запуск `build/art-verification-output`, Microsoft JDK 21.0.11:
**540 unit-тестов, 6 `NativeArtShowcaseTest`, 1 `NativeArenaMarkingTest` — PASS**;
`verifyAssets` — `TECHNICAL_PASS`, 231 файл. Source SHA256:
`515dcfff141944285539b1b43e2ef443f19929be15bf366cce1358a6d35f6d73`.
Разметка всех арен объединена максимум в три статические группы материалов;
проверка сохраняет количество коллизий и положение краски внутри опасных зон.
Это не полный запуск `physicsTest` и не окончательный графический freeze.

В отдельном 1080p-прогоне машин 11 сентября после render-thread PASS обнаружен
`AssertionError` потока `jME3 Audio Decoder`; такой отчёт не считается чистым
завершением. Причина воспроизведена на настоящем `ALAudioRenderer`: удаление
потока оставляет общий `IntBuffer.limit=5`, а запрос `ALC_CONNECTED` ожидает
единственное значение. Локальный `ConnectedQueryAlc` исправляет только окно этого
запроса, сохраняя native ошибки и реальные отключения устройства.
`DesktopAudioSystem` создаёт тот же стандартный renderer; `Main` устанавливает
единственный delegate перед стартом. Тест с настоящим decoder-thread и тестовыми
AL/ALC проверяет продолжение работы после удаления и завершение до уничтожения
устройства. Другой аудиодвижок или второй владелец устройства не вводятся.

Следующий изолированный запуск: **158 presentation/audio-тестов PASS** (включая
70 аудиотестов), `verifyAssets` 231 и `installDist` PASS.
Снимок `build/art-review-snapshot-20260913-0020`, source SHA256:
`fbff8db85b1e2e8881322b7c633135e8f670b8d732aa9c50854924cc3158fe43`.
В активном предупреждении материал фар больше не умножается на 1,5%/7,5%
яркости разбитой лампы: деформированная геометрия сохраняется, обычное затемнение
возвращается по окончании подготовки. Тесты покрывают все пять профилей и четыре
живые стадии повреждения. Настоящие окна на этом снимке проверяются отдельно.
Художественный скрипт включает assertions и отклоняет необработанные исключения
в stdout/stderr; защита проверена на сохранённом логе
`build/three-cars-preview-1080.log`.

Свежая локальная проверка 11 сентября, Microsoft JDK 21.0.11:
`ArenaDynamicPresentationTest` 6/6, `AudioDirectorTest` 29/29,
`NativeArtShowcaseTest` 6/6 и `ArenaMechanismPresentationPhysicsTest` 3/3.
`verifyAssets`: `TECHNICAL_PASS`, 229 файлов. Эти проверки подтверждают исправление
двойного преобразования объединённой площадки и тестовых событий аудио, а также
настоящие подборы и подбросы на шести картах. Общая поставка ещё не завершена:
текущая интеграция предупреждений боссов и последующие графические прогоны проверяются
отдельно; более свежие результаты приведены ниже.

### Повторная проверка 13 сентября

Полный изолированный `test`: **562/562, без failures/errors/skips**;
`verifyAssets`: **231 ресурс, TECHNICAL_PASS**. Source SHA256:
`506fc40877a9022da18d229adfcbd4c26edc9b108fe0eec6f9910835a04e4af5`.
Полный `physicsTest` задачи машин также завершился: **2161/2161**, без ошибок;
его отдельный source SHA256
`d1e6b9fe1844c4ab4d4d6e04ba635e5a4158085c0dfa4d297b835355e1085860`,
реестр `build/three-cars-evidence/physics-results.json`. Это разные снимки,
а не единый финальный release freeze.

Реальные окна выявили ошибку начального отключения bloom: Dead Air Yard мог
оставаться чёрным со старым Loading, хотя события демонстрации продолжались.
Причина — индекс последнего фильтра `FilterPostProcessor`, не обновлённый при
отключении до initialize. `SceneLighting` теперь синхронизирует состояние через
существующий processor, без второго конвейера или постоянно включённого прохода;
значение MSAA=0 нормализуется в допустимый для FPP минимум 1. Четыре регрессионных
теста с настоящим FPP и NullRenderer прошли. Повтор Dead Air 1080p без свечения
показал полноценную сцену во всех четырёх ракурсах.

Сохранены шесть успешных сценариев 720p со свечением и шесть 1080p без свечения.
Все сценарии включали настоящий аудиовыход, `-ea`, восемь подтверждённых типов
подбора, Cannon с фактическим `+1`, ноль недоступных для отрисовки кадров и чистое
завершение без необработанных исключений. На пяти картах кампании зарегистрированы
нативные `LAUNCHED`/`LANDED`. Просмотрены 24 общих ракурса 1080p и шесть overview
720p. Найденный неподвешенный колокол Ash исправлен отдельным повтором ниже.

Первый Construction 1080p был прерван закрытием окна владельцем — это явно
подтверждено им; исходная неудачная запись сохранена. Первый Dead Air 1080p
сохранён как визуальный FAIL несмотря на автоматический PASS. Успешные повторы
имеют отдельные каталоги, исходные диагностические отчёты не переписаны.

Основные матрицы в `build/art-review/`:

- `20260912-202436-515c60ba382d460b9716b3b86fe01649`: прерванный Construction;
- `20260912-202618-faa83730709a4d489f132114ffdc9088`: Construction 1080p;
- `20260912-202738-099db528c137442ba5ed1a14f797cc14`: остальные 1080p, включая неудачный Dead Air;
- `20260912-204157-689305e9df1c455a9d25a7e9fafdbfc9`: исправленный Dead Air 1080p;
- `20260912-204315-e219b4970cbb4afbb5605cad391fe991`: все шесть 720p.
- `20260912-210124-78876992d6c04236879b481ed2e65f04`: исправленный Ash 1080p без свечения;
- `20260912-210213-e2aa6ca739554d6190bbec1bdfe59ec1`: исправленный Ash 720p со свечением.

Матрицы указывают source SHA256/PID/разрешение для каждого результата. Первые
1080p используют снимок `0020`, исправленный Dead Air и вся серия 720p — `0038`.
Это демонстрации с подготовленными позициями и ресурсами, не доказательство
прохождения полного двухуровневого боя или всех фаз пяти боссов.

Завершающий снимок `build/art-review-snapshot-20260913-0101` включает непрерывный
подвес колокола к неподвижной раме и качающемуся корпусу, а также подготовленные
8-bit diffuse-текстуры. Source SHA256:
`9766d53e612b263d2db2db1ac72f72324d8ff020e7ea6e7261dc22b581b7198e`;
JAR SHA256 `456241998D7CC96D242F8CDA1DBD59728F8BBEFA18C1EFD8400F11B3E8F1870D`.
**592/592 unit-теста, 231 ресурс verifyAssets, installDist — PASS**;
три Python-теста offline importer тоже PASS. Проверка одновременного заезда
игрока и бота охватывает все восемь типов подбора. Предшествующий общий запуск
581 теста имел одно устаревшее ожидание MainOptions для вновь разрешённого
`1920x1080`; владелец UI обновил ожидание, полный повтор прошёл.

Два настоящих окна Ash завершились без ошибок: PID 13388/15936, 1080p/720p.
Оба сохранили все восемь подборов, `Cannon +1`, нативные подброс/приземление.
В новых landmark-кадрах лично проверена видимая связь колокола с рамой.

Итоговая локальная галерея:
`build/art-review/collection-20260912-210431/index.html`; реестр `evidence.json`
содержит 12 актуальных случаев и все 16 попыток, включая два неудачных запуска.
Собраны 307 реальных PNG и 14 видео; ссылки на все локальные файлы проверены.
XML 592 unit-тестов и сводка сохранены отдельно в
`build/art-review/unit-after-20260913-0101/`.

Шесть реконструированных 40-секундных аудиодорожек снимка `0020` проверены через
FFmpeg astats: пики от −12,919 до −9,752 dBFS, без отсчётов полного масштаба.
Подробности: `build/art-review/audio-signal-20260913-0020.json`. Это не loopback
OpenAL/HRTF и не подтверждение воспринимаемой громкости владельцем.

### Подготовка diffuse-текстур без изменения изображения

Разбор JFR в задаче выпуска обнаружил синхронное декодирование 16-bit PNG на
первой стадии загрузки. jME 3.8.1 `AWTLoader` всё равно переводит эти источники
в RGB(A)8 через `BufferedImage.getRGB`. Это же преобразование перенесено в
существующий offline importer: четыре diffuse-файла стали 8-bit PNG, исходные
16-bit файлы сохранены; уже 8-bit asphalt не изменён. Размер четырёх runtime
файлов уменьшился с 88 138 244 до 31 767 737 байт. Разрешение 2048², alpha,
порядок пикселей и декодируемые значения каналов сохраняются. Проверка использует
настоящий `AWTLoader` для всех пикселей, обоих вариантов flip и пространства цвета.
Измерение последовательного чтения не заменяет повторный cold-load замер в игре.

Локальное воспроизведение без сетевых запросов:

```powershell
$env:JAVA_HOME='C:\Users\vitalii\.jdks\ms-21.0.11'
python src/tools/import_assets.py --runtime-diffuse-only
```

Нужен имеющийся Python с Pillow/numpy. Helper `PrepareDiffuseTextures.java`
используется только при явной подготовке, не при сборке или запуске игры.
`licenses/asset-provenance.json` сохраняет исходные SHA256/лицензии и фиксирует
новый рецепт и SHA256 производных файлов.

| Проверка | Статус |
|---|---|
| `test`, `physicsTest`, `verifyAssets` на финальной интеграции, Microsoft JDK 21 | PASS на указанных отдельных снимках; финальный freeze проверяет задача выпуска |
| Все восемь моделей: SHA256, происхождение, центр, конечные координаты, габариты, tangent для normal maps | Автоматические проверки PASS |
| События, полный/частичный запас, победитель при одновременном заезде, этажи/видимость, Retry/checkpoint/пауза | Unit/native проверки PASS; не ручное прохождение каждого случая |
| Связь HUD/модель/звук, чужой матч и повторная доставка, 32 источника, переход музыки босса | Автоматические проверки PASS |
| Реальное окно, работающий аудиовыход, все карты при 720p/1080p и выключенном свечении | 12 итоговых сценариев PASS, включая исправленный Ash |
| По карте: 30 с прогрева + 600 с боя; p95 ≤16,7 мс, p99 ≤25 мс, память ≤1,5 GiB, отсутствие боевых кадров >100 мс | PENDING |
| Кадры общего вида и верхнего маршрута; сравнение машин/оружия до/после; запись всех подборов | Сохранены; кадры полного верхнего боя и всех боссов ещё не подтверждены |
| Прослушивание всех сигналов на скорости и в насыщенном реальном боевом миксе | PENDING |

### Промежуточные реальные кадры, 9 сентября 2026

Снимок `build/art-review-snapshot-20260909-1738` прошёл `--art-showcase` на всех
шести аренах при 1280×720 с работающим аудиовыходом и включённым свечением.
Source SHA256:
`23302e345ec269cd90ece73fb8be4303bba56acb227b0ec16f18a615b02ec13f`.
Для каждой карты сохранены четыре общих ракурса, состояния восьми подборов,
реальные `PICKUP` с фактическим количеством и видео. На пяти картах кампании
дополнительно зафиксированы штатные `LAUNCHED` и `LANDED`.

Манифесты находятся в:

- `build/art-review/20260909-133906-ae8524c549834f3a89ec6bb425c362ce/matrix.json`;
- `build/art-review/20260909-134323-499ef61e25bc4ef5b4e3c2440db227cc/matrix.json`.

Первая попытка Euphoria помечена `FAIL`: окно было недоступно для отрисовки
6,8 секунды. Её материалы сохранены; успешный повтор — отдельная запись,
а не изменение исходного результата. Общий снимок предшествует последней
доработке механизмов, разметки подброса и мягких областей огня. Он не доказывает
окончательную интеграцию, бой с боссом, 1080p, выключенное свечение или performance.

Текущие 27 `CombatVisualsTest` отдельно прошли на согласованных исходниках и тестах.
Четыре прежних падения общего запуска происходили между записью новых тестов и
новой реализации: ожидались мягкие patches/48 сегментов кольца, но исполнялась
прежняя геометрия outline/32 сегмента. Проверки плоскости, границы, приоритета
предупреждений и ёмкости буфера не ослаблялись.

### Воспроизведение художественной проверки

Собирать явно на Microsoft JDK 21; `installDist` зафиксировать копией в отдельной
папке перед серией окон. Нельзя подменять JAR во время серии.

```powershell
$env:JAVA_HOME='C:\Users\vitalii\.jdks\ms-21.0.11'
.\gradlew.bat test physicsTest verifyAssets installDist --offline
.\tools\test-art-review.ps1 -InstallDirectory <frozen-install> `
  -JdkHome $env:JAVA_HOME -Ffmpeg <local-ffmpeg> -Resolution 1080p -NoGlow
.\tools\assemble-art-review.ps1 -MatrixDirectories <review-run-1>,<review-run-2> `
  -BaselineDirectory build/visual-baseline-20260909/evidence
```

`test-art-review.ps1` сверяет PID, свежесть отчёта, разрешение, видимость окна,
аудиовыход, идентичность арены и все восемь реальных подборов. Он сохраняет
неудачную попытку и останавливается, не ослабляя критерий. `assemble-art-review.ps1`
собирает локальную галерею и реестр снимков; дополнительные `CombatAfterDirectory`
и `VehicleDirectory` позволяют включить настоящие сравнительные кадры и видео.
Исходный AVI остаётся в каталоге диагностики; MP4 — только кодирование его кадров
с реконструированной дорожкой `AudioCapture`, без дорисовки игрового изображения.

Изолированная проверка реально созданных 18 оружейных WAV выполнена: длительность
0,38–0,55 с, RMS 0,140–0,160, пик ≤0,820, плавные окончания. Это проверка сигнала;
равенство воспринимаемой громкости и художественное качество требуют прослушивания.

Проверен исходный 40-секундный `build/visual-baseline-20260909/evidence/audio.wav`:
стерео 48 kHz/16-bit, пик 0,301880 (−10,403 dBFS), RMS 0,043704,
0 отсчётов полного масштаба из 3 840 000.
SHA256: `383dd6b408f16c3bac74dcd040f68f732ca88967dcfb6748c4b6b2a6aef1d3da`.
Это реконструкция реально выделенных игровых голосов из журнала `AudioCapture`,
не loopback аудиоустройства и не запись микрофона; эффекты HRTF, Doppler и задержки
устройства не воспроизводятся. В исходном журнале нет новых подборов и музыки
кампании, поэтому результат не подтверждает качество или отсутствие клиппинга
окончательного обновлённого микса.

`FEEL_APPROVED` и `MVP_ACCEPTED` остаются предметом фактической приёмки владельцем.
Автоматические проверки, скриншоты и метрики не присваивают эти статусы.
